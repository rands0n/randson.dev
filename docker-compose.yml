services:
  jekyll:
    build: .
    ports:
      - 4000:4000
    volumes:
      - .:/site
    working_dir: /site
    command: bash -c "bundle install && bundle exec jekyll serve --host 0.0.0.0 --livereload"
